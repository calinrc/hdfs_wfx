<project xmlns:ivy="antlib:org.apache.ivy.ant" basedir="./" default="install" name="install">
    <target name="install">
    	<echo message="Start Install process" />
    		<copy todir="${user.home}/.config/doublecmd/plugins">
    			<fileset dir="${basedir}/plugins"/>
    		</copy>
    	<echo message="End Install process" />
    </target>
</project>